{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python backend/run_setup.py && uvicorn app.main:app --host 0.0.0.0 --port=8000"
  }
}
